-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/


-- | Set up ghci with options taken from a .cabal file
--   
--   The executable cabal-ghci runs ghci with the paths and extensions
--   needed by a cabal project. The cabalSet function can be added to your
--   .ghci to provide the same functionality at runtime, giving you more
--   control over the options that are set.
@package cabal-ghci
@version 0.3


-- | Retrieve ghci options for your cabal project
module Distribution.Dev.Interactive

-- | <a>cabalSet</a> returns a list of ghci commands (seperated by
--   newlines) that :set the <a>packageOpts</a> of the current cabal
--   project
cabalSet :: String -> IO String

-- | Build a list of ghci options needed to load files from a cabal project
packageOpts :: FilePath -> PackageDescription -> Maybe LocalBuildInfo -> String -> Maybe [String]

-- | Load the current cabal project file and parse it
loadCabal :: FilePath -> FlagAssignment -> IO LoadCabalRet

-- | Find a .cabal file in the path or any of it's parent directories
lookForCabalFile :: FilePath -> IO (Maybe FilePath)

-- | Generalised version of <a>cabalSet</a>
withOpts :: [String] -> (String -> IO a) -> ([String] -> IO a) -> IO a

-- | Return value for <a>loadCabal</a>
data LoadCabalRet

-- | No cabal file found
NoCabalFile :: LoadCabalRet

-- | Missing dependencies
MissingDeps :: [Dependency] -> LoadCabalRet

-- | Successful loading and parsing of cabal file
Pkg :: FilePath -> PackageDescription -> (Maybe LocalBuildInfo) -> LoadCabalRet
instance Show LoadCabalRet

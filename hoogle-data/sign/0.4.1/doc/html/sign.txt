-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/


-- | Arithmetic over signs and sets of signs
--   
--   Arithmetic over signs (i.e. -, 0, +) and sets of signs
@package sign
@version 0.4.1


-- | This module provides arithmetic over signs (i.e. {-, 0, +}) and set of
--   signs.
--   
--   For the purpose of abstract interpretation, it might be convenient to
--   use <a>Lattice</a> instance. See also lattices package
--   (<a>http://hackage.haskell.org/package/lattices</a>).
module Data.Sign

-- | Signs of real numbers.
data Sign

-- | negative
Neg :: Sign

-- | zero
Zero :: Sign

-- | positive
Pos :: Sign

-- | Unary negation.
negate :: Sign -> Sign

-- | Absolute value.
abs :: Sign -> Sign

-- | Multiplication.
mult :: Sign -> Sign -> Sign

-- | Reciprocal fraction.
recip :: Sign -> Sign

-- | Fractional division.
div :: Sign -> Sign -> Sign

-- | Fractional division.
pow :: Integral x => Sign -> x -> Sign

-- | Sign of a number.
signOf :: Real a => a -> Sign

-- | Mnemonic symbol of a number.
--   
--   This function returns <tt>"-"</tt>, <tt>"0"</tt>, <tt>"+"</tt>
--   respectively for <a>Neg</a>, <a>Zero</a>, <a>Pos</a>.
symbol :: Sign -> String
instance Typeable Sign
instance Eq Sign
instance Ord Sign
instance Show Sign
instance Read Sign
instance Enum Sign
instance Bounded Sign
instance Data Sign
instance Fractional (Set Sign)
instance Num (Set Sign)
instance BoundedLattice (Set Sign)
instance BoundedMeetSemiLattice (Set Sign)
instance Lattice (Set Sign)
instance MeetSemiLattice (Set Sign)
instance Enumerable Sign
instance Hashable Sign
instance NFData Sign

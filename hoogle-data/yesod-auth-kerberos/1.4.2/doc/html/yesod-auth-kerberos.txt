-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/


-- | Kerberos Authentication for Yesod.
--   
--   Kerberos Authentication for Yesod.
@package yesod-auth-kerberos
@version 1.4.2


-- | In-built kerberos authentication for Yesod.
--   
--   Please note that all configuration should have been done manually on
--   the machine prior to running the code.
--   
--   On linux machines the configuration might be in <i>etc</i>krb5.conf.
--   It's worth checking if the Kerberos service provider (e.g. your
--   university) already provide a complete configuration file.
--   
--   Be certain that you can manually login from a shell by typing
--   
--   <pre>
--   kinit username
--   </pre>
--   
--   If you fill in your password and the program returns no error code,
--   then your kerberos configuration is setup properly. Only then can this
--   module be of any use.
module Yesod.Auth.Kerberos

-- | Kerberos with <a>defaultKerberosConfig</a>
authKerberos :: YesodAuth m => AuthPlugin m

-- | A configurable version of <a>authKerberos</a>
genericAuthKerberos :: YesodAuth m => KerberosConfig -> AuthPlugin m
data KerberosConfig
KerberosConfig :: (Text -> Text) -> (Text -> Text) -> KerberosConfig

-- | When a user gives username x, f(x) will be passed to Kerberos
usernameModifier :: KerberosConfig -> Text -> Text

-- | When a user gives username x, f(x) will be passed to Yesod
identifierModifier :: KerberosConfig -> Text -> Text

-- | A configuration where the username the user provides is the one passed
--   to both kerberos and yesod
defaultKerberosConfig :: KerberosConfig

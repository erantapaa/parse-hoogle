-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/


-- | Image viewer extension for Manatee.
--   
--   manatee-imageviewer is image viewer extension for Manatee
--   (Haskell/Gtk+ Integrated Live Environment)
@package manatee-imageviewer
@version 0.0.2

module Manatee.Extension.ImageViewer.PageMode

-- | Image mode.
imageMode :: PageMode

module Manatee.Extension.ImageViewer.ImageBuffer
data ImageBuffer
ImageBuffer :: TVar String -> Client -> PageId -> PageMode -> TVar [FilePath] -> TChan String -> TVar (Maybe HandlerId) -> ImageBuffer
imageBufferPath :: ImageBuffer -> TVar String
imageBufferClient :: ImageBuffer -> Client
imageBufferPageId :: ImageBuffer -> PageId
imageBufferMode :: ImageBuffer -> PageMode
imageBufferFiles :: ImageBuffer -> TVar [FilePath]
imageBufferBroadcastChannel :: ImageBuffer -> TChan String
imageBufferSlideShowHanlderId :: ImageBuffer -> TVar (Maybe HandlerId)

-- | New image buffer.
imageBufferNew :: String -> Client -> PageId -> IO ImageBuffer

-- | Listen broadcast channel.
imageBufferListenChannel :: ImageBuffer -> IO ()

-- | Scan image files under current directory. Run this function in
--   back-end thread, then user can open image immediately and don't need
--   wait scan finish.
imageBufferScanFiles :: ImageBuffer -> IO ()
instance Typeable ImageBuffer

module Manatee.Extension.ImageViewer.ImageView
data ImageView
ImageView :: TVar PagePlugId -> ScrolledWindow -> ImageView -> ImageBuffer -> ViewChannel String -> ImageView
imageViewPlugId :: ImageView -> TVar PagePlugId
imageViewScrolledWindow :: ImageView -> ScrolledWindow
imageViewView :: ImageView -> ImageView
imageViewBuffer :: ImageView -> ImageBuffer
imageViewBroadcastChannel :: ImageView -> ViewChannel String

-- | The intervals of slide show (ms).
imageViewSlideShowInterval :: Int

-- | New image view.
imageViewNew :: ImageBuffer -> PagePlugId -> IO ImageView

-- | Draw image view.
imageViewDraw :: ImageView -> IO ()

-- | Browse.
imageViewBrowse :: ImageView -> FilePath -> IO ()

-- | Listen broadcast channel for draw view synchronous.
imageViewListenChannel :: ImageView -> IO ()

-- | Handle key action.
imageViewHandleKeyAction :: ImageView -> Text -> SerializedEvent -> IO ()

-- | Zoom out image.
imageViewZoomOut :: ImageView -> IO ()

-- | Zoom int image.
imageViewZoomIn :: ImageView -> IO ()

-- | Fit the window.
imageViewFit :: ImageView -> IO ()

-- | View next file.
imageViewBrowseNext :: ImageView -> IO ()

-- | View Prev file.
imageViewBrowsePrev :: ImageView -> IO ()

-- | View first file.
imageViewBrowseFirst :: ImageView -> IO ()

-- | View last file.
imageViewBrowseLast :: ImageView -> IO ()

-- | Rotate clockwise
imageViewRotateClockwise :: ImageView -> IO ()

-- | Rotate counterclockwise.
imageViewRotateCounterclockwise :: ImageView -> IO ()

-- | Rotate mirror.
imageViewRotateMirror :: ImageView -> IO ()

-- | Update zoom status.
imageViewUpdateZoomStatus :: ImageView -> IO ()

-- | Find next image file under current directory.
imageViewGetNextFile :: FilePath -> [FilePath] -> Maybe FilePath

-- | Find previous image file under current directory.
imageViewGetPrevFile :: FilePath -> [FilePath] -> Maybe FilePath

-- | Slide show.
imageViewSlideShow :: ImageView -> IO ()

-- | Set as background, (just work for gnome).
imageViewSetAsBackground :: ImageView -> IO ()

-- | Keymap.
imageViewKeymap :: Map Text (ImageView -> IO ())
instance Typeable ImageView
instance PageView ImageView
instance PageBuffer ImageBuffer

module Manatee.Extension.ImageViewer

-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/


-- | Snap framework snaplet for the AMQP library
--   
--   
--   `snaplet-amqp` is a snaplet for the Snap web framework providing
--   convenience functions and state management for the Haskell AMQP
--   package.
--   
--   Please refer to the README for an example - Cabal descriptions don't
--   make it easy to do so.
@package snaplet-amqp
@version 1.1.0.0

module Snap.Snaplet.AMQP

-- | Initialize the AMQP Snaplet.
initAMQP :: SnapletInit b AmqpState

-- | Runs an AMQP action in any monad with a HasAmqpPoolonn instance.
runAmqp :: HasAmqpPool m => (Channel -> IO ()) -> m ()

-- | Constructs a connect from Config.
mkAmqpPool :: MonadIO m => Config -> m (AmqpPool, Connection)
newtype AmqpState
AmqpState :: AmqpPool -> AmqpState
amqpPool :: AmqpState -> AmqpPool
class MonadIO m => HasAmqpPool m
getAmqpPool :: HasAmqpPool m => m AmqpPool
instance MonadIO m => HasAmqpPool (ReaderT AmqpPool m)
instance HasAmqpPool (Handler b AmqpState)

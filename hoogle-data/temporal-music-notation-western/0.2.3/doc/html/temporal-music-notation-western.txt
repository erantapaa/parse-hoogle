-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/


-- | western music notation
--   
--   Localization of 'temporal-music-notation' library for Western music
--   tradition.
@package temporal-music-notation-western
@version 0.2.3


-- | Names specific to western music tradition
module Temporal.Music.Western
pppiano :: VolumeLike a => Score a -> Score a
ppiano :: VolumeLike a => Score a -> Score a
piano :: VolumeLike a => Score a -> Score a
mpiano :: VolumeLike a => Score a -> Score a
mforte :: VolumeLike a => Score a -> Score a
forte :: VolumeLike a => Score a -> Score a
fforte :: VolumeLike a => Score a -> Score a
ffforte :: VolumeLike a => Score a -> Score a
ppp' :: VolumeLike a => Score a -> Score a
pp' :: VolumeLike a => Score a -> Score a
p' :: VolumeLike a => Score a -> Score a
mp' :: VolumeLike a => Score a -> Score a
mf' :: VolumeLike a => Score a -> Score a
f' :: VolumeLike a => Score a -> Score a
ff' :: VolumeLike a => Score a -> Score a
fff' :: VolumeLike a => Score a -> Score a

-- | diminuendo
dim :: VolumeLike a => Accent -> Score a -> Score a

-- | crescendo
cresc :: VolumeLike a => Accent -> Score a -> Score a

-- | rondo form
--   
--   <pre>
--   rondo a b c = line [a, b, a, c, a]
--   </pre>
rondo :: Score a -> Score a -> Score a -> Score a

-- | reprise form
--   
--   <pre>
--   reprise a b1 b2 = line [a, b1, a, b2]
--   </pre>
reprise :: Score a -> Score a -> Score a -> Score a
type Tempo = Double

-- | very slow (40-60 bpm), like largo
lento :: Double -> Tempo

-- | very slow (40-60 bpm)
largo :: Double -> Tempo

-- | rather broadly (60-66 bpm)
larghetto :: Double -> Tempo

-- | slow and sloemn (60 - 66 bpm)
grave :: Double -> Tempo

-- | slow and stately (literally <a>at ease</a>) (66-76 bpm)
adagio :: Double -> Tempo

-- | rather slow (70-80 bpm)
adagietto :: Double -> Tempo

-- | at awalking pace (76-80 bpm)
andante :: Double -> Tempo

-- | slightly faster then andante (80-100 bpm)
andantino :: Double -> Tempo

-- | moderately (101-110 bpm)
moderato :: Double -> Tempo

-- | moderately fast (115-125 bpm)
allegretto :: Double -> Tempo

-- | fast, at 'march tempo' (120-139 bpm)
allegro :: Double -> Tempo

-- | lively and fast (135-160 bpm)
vivace :: Double -> Tempo

-- | very fast (168-200 bpm)
presto :: Double -> Tempo

-- | extremely fast (200 - 230 bpm)
prestissimo :: Double -> Tempo


-- | Western twelve-tone scale.
module Temporal.Music.Western.P12

-- | Constructs <a>Score</a> with one note. <a>Scale</a> is set to equal
--   tempered scale (from <a>c1</a>).
tone :: Step -> Score (Note a)
c :: Score (Note a)
d :: Score (Note a)
e :: Score (Note a)
f :: Score (Note a)
g :: Score (Note a)
a :: Score (Note a)
b :: Score (Note a)
cs :: Score (Note a)
ds :: Score (Note a)
es :: Score (Note a)
fs :: Score (Note a)
gs :: Score (Note a)
as :: Score (Note a)
bs :: Score (Note a)
cf :: Score (Note a)
df :: Score (Note a)
ef :: Score (Note a)
ff :: Score (Note a)
gf :: Score (Note a)
af :: Score (Note a)
bf :: Score (Note a)

-- | 12 tone equal temperament scale
eqt :: Hz -> Scale

-- | pythagorean scale
pyth :: Hz -> Scale

-- | hindemithean scale with mean of fs and gb for tritone
hind :: Hz -> Scale

-- | hindemithean scale with fs for tritone
hindFs :: Hz -> Scale

-- | hindemithean scale with gb for tritone
hindGb :: Hz -> Scale


-- | Bohlen-Pierce tones
module Temporal.Music.Western.P13

-- | Constructs <a>Score</a> with one note. <a>Scale</a> is set to equal
--   tempered Bohlen-Pierce scale (from <a>c1</a>).
tone :: Step -> Score (Note a)
c :: Score (Note a)
cs :: Score (Note a)
cf :: Score (Note a)
d :: Score (Note a)
ds :: Score (Note a)
df :: Score (Note a)
e :: Score (Note a)
es :: Score (Note a)
ef :: Score (Note a)
f :: Score (Note a)
fs :: Score (Note a)
ff :: Score (Note a)
g :: Score (Note a)
gs :: Score (Note a)
gf :: Score (Note a)
h :: Score (Note a)
hs :: Score (Note a)
hf :: Score (Note a)
j :: Score (Note a)
js :: Score (Note a)
jf :: Score (Note a)
a :: Score (Note a)
as :: Score (Note a)
af :: Score (Note a)
b :: Score (Note a)
bs :: Score (Note a)
bf :: Score (Note a)

-- | equal Bohlen-Pierce scale
eqBP :: Hz -> Scale

-- | just Bohlen-Pierce scale
justBP :: Hz -> Scale

-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/


-- | Image IO for Yarr library
--   
--   <tt>repa-devil</tt> package, adapted to <tt>yarr</tt> library.
@package yarr-image-io
@version 1.3.2.1

module Data.Yarr.IO.Image
data Image
RGBA :: (UArray F L Dim2 (VecList N4 Word8)) -> Image
RGB :: (UArray F L Dim2 (VecList N3 Word8)) -> Image
BGRA :: (UArray F L Dim2 (VecList N4 Word8)) -> Image
BGR :: (UArray F L Dim2 (VecList N3 Word8)) -> Image
Grey :: (UArray F L Dim2 Word8) -> Image

-- | 8-bit unsigned integer type
data Word8 :: *

-- | Reads <a>Image</a> from file.
readImage :: FilePath -> IO Image

-- | Writes <a>Image</a> to file.
writeImage :: FilePath -> Image -> IO ()

-- | Fuction to uniformly import images of any type.
--   
--   Example:
--   
--   <pre>
--   anyImage &lt;- <a>readImage</a> "lena.png"
--   let image = readRGB (\r g b -&gt; ...) anyImage
--   </pre>
readRGB :: (Fun N3 Word8 a) -> Image -> UArray D L Dim2 a

-- | <pre>
--   readRGBVectors = <a>readRGB</a> <a>construct</a>
--   </pre>
readRGBVectors :: (Vector v Word8, Dim v ~ N3) => Image -> UArray D L Dim2 (v Word8)
instance Show ImageName
instance Show Image

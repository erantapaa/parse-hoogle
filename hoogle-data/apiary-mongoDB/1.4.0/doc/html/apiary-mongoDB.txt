-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/


-- | mongoDB support for apiary web framework.
--   
@package apiary-mongoDB
@version 1.4.0

module Web.Apiary.MongoDB
data MongoDB
data MongoDBConfig
MongoDBConfig :: Secs -> Host -> Maybe (Username, Password) -> Database -> AccessMode -> Int -> NominalDiffTime -> MongoDBConfig
mongoDBTimeout :: MongoDBConfig -> Secs
mongoDBHost :: MongoDBConfig -> Host
mongoDBAuth :: MongoDBConfig -> Maybe (Username, Password)
mongoDBDatabase :: MongoDBConfig -> Database
mongoDBAccessMode :: MongoDBConfig -> AccessMode
numConnection :: MongoDBConfig -> Int
connectionIdleTime :: MongoDBConfig -> NominalDiffTime
type MongoQuery = Query
initMongoDB :: (MonadIO m, MonadBaseControl IO m) => MongoDBConfig -> Initializer' m MongoDB

-- | initialize MongoDB extension using heroku service.
--   
--   compatible:
--   
--   <ul>
--   <li>MongoHQ</li>
--   <li>MongoLab</li>
--   <li>MongoSoup</li>
--   </ul>
initHerokuMongoDB :: (MonadIO m, MonadBaseControl IO m, Has Heroku exts) => MongoDBConfig -> Initializer m exts (MongoDB : exts)

-- | query using <a>MongoDBConfig</a> settings.
--   
--   if you want to access other db, other accessmode, please use
--   <a>useDb</a> or <a>accessMode</a>.
access :: (MonadExts es m, Has MongoDB es, MonadBaseControl IO m, MonadIO m) => Action m a -> m a
instance Default MongoDBConfig
instance Extension MongoDB

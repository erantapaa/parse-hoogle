-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/


-- | A truncator for blaze-html
--   
--   This package provides a simple function to truncate HTML, preserving
--   tags and preventing cut-off words.
@package blaze-html-truncate
@version 0.3.0.0

module Text.Blaze.Truncate

-- | Truncate the given HTML to a certain length, preserving tags. Returns
--   the truncated Html or <a>Nothing</a> if no truncation occured. Words
--   are preserved, so if the truncated text ends within some word, that
--   whole word is cut.
truncateHtml :: Int -> Markup -> Maybe Markup
instance Functor Tagged

-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/


-- | breaks strings to fit width
--   
--   Simple functions to break a String to fit a maximum text width, using
--   Knuth-Liang hyphenation algorhitm.
@package linebreak
@version 1.0.0.3


-- | Simple functions to break a String to fit a maximum text width, using
--   Knuth-Liang hyphenation algorithm.
--   
--   Example:
--   
--   <pre>
--   import Text.Hyphenation
--   import Text.LineBreak
--   
--   hyp = Just english_US
--   bf = BreakFormat 25 4 '-' hyp
--   cs = "Using hyphenation with gruesomely non parsimonious wording."
--   main = putStr $ breakString bf cs
--   </pre>
--   
--   will output:
--   
--   <pre>
--   Using hyphenation with
--   gruesomely non parsimo-
--   nious wording.
--   </pre>
module Text.LineBreak

-- | Breaks some text (String) to make it fit in a certain width. The
--   output is a String, suitable for writing to screen or file.
breakString :: BreakFormat -> String -> String

-- | Convenience for <tt>lines $ breakString bf cs</tt>
breakStringLn :: BreakFormat -> String -> [String]

-- | How to break the strings: maximum width of the lines, number of spaces
--   to replace tabs with (dumb replacement), symbol to use to hyphenate
--   words, hypenator to use (language, exceptions, etc.; refer to
--   <a>Text.Hyphenation</a> for usage instructions). To break lines
--   without hyphenating, put <tt>Nothing</tt> in <tt>bfHyphenator</tt>.
data BreakFormat
BreakFormat :: Int -> Int -> Char -> Maybe Hyphenator -> BreakFormat
bfMaxCol :: BreakFormat -> Int
bfTabRep :: BreakFormat -> Int
bfHyphenSymbol :: BreakFormat -> Char
bfHyphenator :: BreakFormat -> Maybe Hyphenator
instance Show Element

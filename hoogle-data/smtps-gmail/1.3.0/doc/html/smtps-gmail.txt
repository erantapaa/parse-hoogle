-- Hoogle documentation, generated by Haddock
-- See Hoogle, http://www.haskell.org/hoogle/


-- | Gmail SMTP Client
--   
@package smtps-gmail
@version 1.3.0


-- | A dead simple SMTP Client for sending Gmail.
module Network.Mail.Client.Gmail

-- | Send an email from your Gmail account using the simple message
--   transfer protocol with transport layer security. If you have 2-step
--   verification enabled on your account, then you will need to retrieve
--   an application specific password before using this function. Below is
--   an example using ghci, where Alice sends an Excel spreadsheet to Bob.
--   
--   <pre>
--   &gt;&gt;&gt; :set -XOverloadedStrings
--   &gt;&gt;&gt; :module Network.Mail.Mime Network.Mail.Client.Gmail
--   &gt;&gt;&gt; sendGmail "alice" "password" (Address (Just "Alice") "alice@gmail.com") [Address (Just "Bob") "bob@example.com"] [] [] "Excel Spreadsheet" "Hi Bob,\n\nThe Excel spreadsheet is attached.\n\nRegards,\n\nAlice" ["Spreadsheet.xls"] 10000000
--   </pre>
sendGmail :: Text -> Text -> Address -> [Address] -> [Address] -> [Address] -> Text -> Text -> [FilePath] -> Int -> IO ()
data GmailException
ParseError :: String -> GmailException
Timeout :: GmailException
instance Typeable GmailException
instance Show GmailException
instance Exception GmailException
